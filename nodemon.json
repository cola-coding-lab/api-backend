{
  "watch": ["src"],
  "ext": "ts",
  "exec": "npx ts-node --project tsconfig.dev.json -r tsconfig-paths/register ./src/server.ts",
  "env": {
    "NODEMON": "development"
  },
  "ignore": ["src/assets"]
}
