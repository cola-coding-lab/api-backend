version: '3'

services:
  express-ts:
    build:
      context: .
      dockerfile: Dockerfile
    image: cola-api-service
    container_name: cola-api-service
    restart: unless-stopped
    ports:
      - "8065:8065"
    environment:
      - NODE_ENV=production
      - SERVICE_PORT=8065

